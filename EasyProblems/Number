var countSeniors = function(details) {
    let count = 0
    for (const detail of details){
        const age = parseInt(detail.slice(11,13),10)
        if(age >60){
            count++
        }
    }
    return count
};


this i used python i faced and error so i need to fix this so i used javascript to do it 

